import useStore from "@/store";
import React from "react";
import { Navigate } from "react-router-dom";

function Attendance() {
	return (
		<div>
			<h1 className="font-bold text-5xl">Attendance</h1>
		</div>
	);
}

export default Attendance;
