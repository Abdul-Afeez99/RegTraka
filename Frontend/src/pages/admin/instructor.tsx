import useStore from "@/store";
import React from "react";
import { Navigate } from "react-router-dom";

function Instructor() {
	return (
		<div>
			<h1 className="font-bold text-5xl">Instructor</h1>
		</div>
	);
}

export default Instructor;
